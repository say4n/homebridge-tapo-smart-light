# Release Workflow

1. Bump the version number up based on if the release is a major, minor or patch.
2. Create a new git commit and a git tag for the commit for the version.
3. Push both the commit and the tag to the git remote.
